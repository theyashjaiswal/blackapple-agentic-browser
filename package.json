{
  "name": "blackapple-agentic-browser",
  "version": "0.1.0",
  "private": true,
  "description": "BlackApple Agentic Browser — headless browser for AI agent orchestration",
  "scripts": {
    "build": "npm run build --prefix packages/core && npm run build --prefix packages/api && npm run build --prefix packages/sdk && npm run build --prefix cli",
    "dev": "npm run dev --prefix packages/api"
  }
}
